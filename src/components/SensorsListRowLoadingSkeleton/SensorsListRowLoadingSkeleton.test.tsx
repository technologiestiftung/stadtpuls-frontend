import { render } from "@testing-library/react";
import { SensorsListRowLoadingSkeleton } from ".";

describe("SensorsListRowLoadingSkeleton", () => {
  it("renders correctly", () => {
    render(<SensorsListRowLoadingSkeleton />);
  });
});
